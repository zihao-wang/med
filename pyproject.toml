[project]
name = "med"
version = "0.1.0"
description = "Minimal Embeddable Dimension — ICML 2026 companion code"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "numpy",
    "matplotlib",
    "tqdm",
    "cvxpy",
]

[dependency-groups]
dev = []
